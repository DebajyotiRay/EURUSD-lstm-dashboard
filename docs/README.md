Put your dashboard screenshot here as `screenshot.png` (referenced from the main README).
A quick way to generate one: run the app locally, open it in a browser, and take a screenshot
of the loaded dashboard with a live prediction showing.
